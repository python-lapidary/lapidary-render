def {{ auth_name }}(api_key: str) -> NamedAuth:
    return '{{ auth_name }}', httpx_auth.{{ auth.location.value | capitalize }}ApiKey(
        api_key='{{ auth.format }}'.format(api_key),
        header_name='{{ auth.name }}',
    )
