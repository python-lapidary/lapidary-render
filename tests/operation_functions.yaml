openapi: 3.0.3
info:
  title: test schema
  version: 1.0.0
components:
  parameters:
    comp_param:
      name: comp_param
      in: cookie
      schema:
        type: string
paths:
  /simple-response/:
    parameters:
    - name: c1
      schema:
        type: string
      in: header
    get:
      operationId: get_simple_response
      responses:
        '200':
          description: test response
          content:
            application/json:
              schema:
                type: string
      parameters:
      - name: a
        schema:
          type: string
        in: query
  /ref-params/:
    parameters:
    - name: c2
      schema:
        type: string
      in: header
    get:
      operationId: get_ref_params
      responses:
        'default':
          description: test response
          content:
            application/json:
              schema:
                type: string
      parameters:
      - $ref: '#/components/parameters/comp_param'
      - $ref: '#/paths/~1simple-response~1/parameters/0'
      - $ref: '#/paths/~1simple-response~1/get/parameters/0'

  /schema-response/:
    get:
      operationId: get_schema_response
      responses:
        '200':
          description: test response
          content:
            application/json:
              schema:
                type: object
                properties:
                  a:
                    type: string
                  b:
                    type: string
                additionalProperties: false
  /schema-request/:
    get:
      operationId: get_schema_request
      responses:
        default:
          description: ''
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                a:
                  type: string
                b:
                  type: string
              additionalProperties: false
  /ignored-header/:
    get:
      operationId: ignored_header
      responses:
        default:
          description: ''
      parameters:
      - name: accept
        in: header
        schema:
          type: string
