openapi: '3.0.3'
info:
  title: Lapidary test schema
  version: 1.0.0
paths: { }
components:
  schemas:
    NonSpaceName:
      additionalProperties: false
      type: object
      properties:
        random property:
          type: object
          additionalProperties: false
          properties:
            key:
              type: string
          required:
          - key
      required:
      - random property
      x-lapidary-names:
        random property: random_property
