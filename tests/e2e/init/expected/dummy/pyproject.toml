# This file was generated but won't be updated automatically and may be edited manually.

[build-system]
build-backend = "poetry.core.masonry.api"
requires = [
    "poetry-core>=1.3.2",
]

[tool.poetry]
name = "dummy_package"
description = "Client library for dummy"
version = "0.1.0"
authors = []
license = ""
packages = [
    { include = "dummy_package", from = "src" },
]

[tool.poetry.dependencies]
python = "^3.9"
lapidary = "^0.12.0"

[tool.lapidary]
document_path = "lapidary/openapi/dummy.yaml"
package = "dummy_package"
